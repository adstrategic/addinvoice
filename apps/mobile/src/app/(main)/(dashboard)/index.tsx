import { ComingSoon } from '@/components/shared/coming-soon'

/** Placeholder — the real dashboard is phase 9 (KPI tiles, revenue chart, recent activity). */
export default function DashboardScreen() {
	return (
		<ComingSoon
			title="Home"
			description="Your revenue chart, KPI tiles and recent activity will live here."
		/>
	)
}
