import { ComingSoon } from '@/components/shared/coming-soon'

/** Placeholder — Invoices is phase 6. Becomes a directory with its own Stack then. */
export default function InvoicesScreen() {
	return (
		<ComingSoon
			title="Invoices"
			description="Create, send and get paid on invoices from your phone. Building this next."
		/>
	)
}
