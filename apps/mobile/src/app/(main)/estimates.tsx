import { ComingSoon } from '@/components/shared/coming-soon'

/** Placeholder — Estimates is phase 8. Becomes a directory with its own Stack then. */
export default function EstimatesScreen() {
	return (
		<ComingSoon
			title="Estimates"
			description="Quotes, descriptive items and client acceptance will live here."
		/>
	)
}
